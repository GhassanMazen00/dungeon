# Images

Put your portrait here as `ghassan.jpg`.

The hero picks it up automatically — see `PROFILE.photo` in
`assets/js/data/site.js` if you want a different filename or a hosted URL.

If the file is absent the hero falls back to the pixel avatar, so the page never
renders a broken image.
